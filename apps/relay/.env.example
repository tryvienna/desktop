# JWT secret for verifying desktop session tokens.
# In dev, leave empty to skip signature verification (tokens are decoded only).
# In production, must match the web app's DESKTOP_JWT_SECRET.
# DESKTOP_JWT_SECRET=

# Server port (Railway sets PORT automatically)
PORT=3300
