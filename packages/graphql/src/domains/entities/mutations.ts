/**
 * Entity Mutations — Generic mutation fields.
 *
 * Entity-specific mutations are defined by plugins via `entityPayload()` + `mutationFields()`.
 *
 * @module graphql/domains/entities/mutations
 */

// No generic entity mutations — all API operations go through plugin GraphQL schemas.
